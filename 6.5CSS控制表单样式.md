### 一、使用CSS控制表单样式

为了实现结构表现相互分离和效果的多样化，我们一般使用CSS来控制表单的样式。

**CSS控制表单的注意事项**

>  1.由于`form`是块元素，充值浏览器的默认样式的时候需要清除其内外边距。
>
> 2.使用`<input/>`标记定义各种按钮时需要清除其边框。
>
> 3.通常情况下需要对文本框和密码框设置2到3像素的*内边距*，使输入内容不会紧贴输入框。

